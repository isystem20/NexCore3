@using NEXARC.Web.Views.Shared.Components.TopBarUserMenu
@model TopBarUserMenuViewModel

<li class="dropdown user user-menu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <img src="@Url.Content("~/images/user.png")" class="user-image" alt="User Image">
        <span class="hidden-xs">@Html.Raw(Model.GetShownLoginName())</span>
    </a>
    <ul class="dropdown-menu">
        <!-- User image -->
        <li class="user-header">
            <img src="@Url.Content("~/images/user.png")" class="img-circle" alt="User Image">
            <p>
                @Html.Raw(Model.GetShownLoginName())
                <small>@Model.LoginInformations.User.EmailAddress</small>
            </p>
        </li>
        <!-- Menu Body -->
        @*<li class="user-body">
            <div class="row">
                <div class="col-xs-4 text-center">
                    <a href="#">Link1</a>
                </div>
                <div class="col-xs-4 text-center">
                    <a href="#">Link2</a>
                </div>
                <div class="col-xs-4 text-center">
                    <a href="#">Link3</a>
                </div>
            </div>
            <!-- /.row -->
        </li>*@
        <!-- Menu Footer-->
        <li class="user-footer">
            <div class="pull-left">
                @*<a href="#" class="btn btn-default btn-flat">Button1</a>*@
            </div>
            <div class="pull-right">
                <a class="btn btn-default btn-flat" href="@Url.Action("Logout", "Account", new { area = string.Empty })">
                    @L("Logout")
                </a>
            </div>
        </li>
    </ul>
</li>
