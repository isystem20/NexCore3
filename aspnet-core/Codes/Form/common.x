                <div class="form-group">
                    <label for="{{PropertyName}}">{{PropertyName}}</label>
                    <input required="true" id="{{PropertyName}}" class="form-control form-control-sm"
                            name="{{PropertyName}}" placeholder="{{PropertyName}}"
                            value="@Model.{{Entity}}.{{PropertyName}}" />
                </div>